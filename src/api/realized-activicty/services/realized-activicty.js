'use strict';

/**
 * realized-activicty service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::realized-activicty.realized-activicty');
