'use strict';

/**
 * realized-activicty router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::realized-activicty.realized-activicty');
