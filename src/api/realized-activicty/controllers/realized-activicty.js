'use strict';

/**
 * realized-activicty controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::realized-activicty.realized-activicty');
