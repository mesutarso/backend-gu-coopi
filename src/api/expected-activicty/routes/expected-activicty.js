'use strict';

/**
 * expected-activicty router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::expected-activicty.expected-activicty');
